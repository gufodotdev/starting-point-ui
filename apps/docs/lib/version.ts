import { version } from "starting-point-ui/package.json";

// The installed package version, for the header badge and %VERSION%
// placeholders in mdx content (e.g. the cdn snippet in the installation
// guide), so the docs can never drift from the release they document.
export { version };
