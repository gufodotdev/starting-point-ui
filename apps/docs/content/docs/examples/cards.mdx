---
title: Tailwind CSS Card Examples
description: Beautiful, modern card examples for Tailwind CSS. Copy a login form, a product card, a pricing card, a profile card, and more. Easily customizable.
---

Ready-made cards for Tailwind CSS, every one built on the [card](/components/card) component with the library's form and content pieces inside. Click an example to open its page and copy the code.

## Login Form

A login form for Tailwind CSS with email and password fields, a forgot password link, and social sign in. Built with the [card](/components/card) and [field](/components/field) components.

```html preview preset=lg
%include examples/cards/login.tsx%
```

## Sign Up Form

A sign up form for Tailwind CSS with email and password fields, social providers, and a link to log in. Built with the [card](/components/card) and [field](/components/field) components.

```html preview preset=lg
%include examples/cards/sign-up.tsx%
```

## Product Card

A product card for Tailwind CSS with a cover photo, star rating, price, and add to cart button. Built with the [card](/components/card) and [badge](/components/badge) components.

```html preview preset=lg
%include examples/cards/product.tsx%
```

## Blog Card

A blog card for Tailwind CSS with a cover image, title, category, and author. Built with the [card](/components/card) and [avatar](/components/avatar) components.

```html preview preset=lg
%include examples/cards/blog-post.tsx%
```

## Share Dialog

A share dialog for Tailwind CSS with a copyable link and per-person access levels. Built with the [card](/components/card), [avatar](/components/avatar), and [select](/components/select) components.

```html preview preset=lg
%include examples/cards/share-document.tsx%
```

## Payment Method Form

A payment method form for Tailwind CSS where each method is a selectable radio choice card. Built with the [card](/components/card) and [field](/components/field) components.

```html preview preset=lg
%include examples/cards/payment-method.tsx%
```

## Notification Settings

A notification settings card for Tailwind CSS with a switch, label, and description per option. Built with the [card](/components/card) and [switch](/components/switch) components.

```html preview preset=base
%include examples/cards/notifications.tsx%
```

## Feedback Form

A feedback form for Tailwind CSS with a type select, a title input, and a message textarea. Built with the [card](/components/card), [select](/components/select), and [textarea](/components/textarea) components.

```html preview preset=lg
%include examples/cards/feedback.tsx%
```

## Pricing Card

A pricing card for Tailwind CSS with the plan name, monthly price, a feature checklist, and an upgrade button. Built with the [card](/components/card) component.

```html preview preset=lg
%include examples/cards/premium-plan.tsx%
```

## Profile Card

A profile card for Tailwind CSS with a photo, bio, follower stats, and a follow button. Built with the [card](/components/card) and [button](/components/button) components.

```html preview preset=lg
%include examples/cards/profile.tsx%
```

## Testimonial Card

A testimonial card for Tailwind CSS with a star rating, a quote, and the reviewer's photo, name, and role. Built with the [card](/components/card) and [avatar](/components/avatar) components.

```html preview preset=base
%include examples/cards/testimonial.tsx%
```

## Music Player Card

A music player card for Tailwind CSS with cover art, track progress, and play, previous, and next controls. Built with the [card](/components/card) and [progress](/components/progress) components.

```html preview preset=base h=44rem
%include examples/cards/music-player.tsx%
```

## Chat UI

A chat UI for Tailwind CSS with streaming assistant replies, a scrolling thread, and a message composer. Built with the [card](/components/card) and [message scroller](/components/message-scroller) components.

```html preview preset=base h=42rem
%include examples/cards/chat.tsx%
```

## Shopping Cart

A shopping cart for Tailwind CSS with product thumbnails, quantities, a subtotal, and a checkout button. Built with the [card](/components/card) and [button](/components/button) components.

```html preview preset=lg
%include examples/cards/shopping-cart.tsx%
```

## Payment Success

A payment success card for Tailwind CSS with a success icon, the amount and reference, and stacked actions. Built with the [card](/components/card) and [button](/components/button) components.

```html preview preset=base
%include examples/cards/invoice-paid.tsx%
```

## Radio Group with Nested Checkboxes

A radio group for Tailwind CSS with nested checkboxes that CSS alone disables under an unpicked option. Built with the [radio group](/components/radio-group) and [checkbox](/components/checkbox) components.

```html preview preset=lg
%include examples/cards/attendance.tsx%
```
