---
title: Tailwind CSS Dropdown Examples
description: Beautiful, modern dropdown menu examples for Tailwind CSS. Copy a profile menu, a notifications panel, a country dropdown, or a mega menu. Easily customizable.
---

Ready-made dropdown menus for Tailwind CSS, built with the [dropdown](/components/dropdown), [popover](/components/popover), and [combobox](/components/combobox) components. Click an example to open its page and copy the code.

## Profile Dropdown

A profile dropdown for Tailwind CSS, the user menu behind an avatar with an account header and shortcuts. Built with the [dropdown](/components/dropdown) and [avatar](/components/avatar) components.

```html preview preset=lg align=top open=.dropdown
%include examples/dropdowns/profile-dropdown.tsx%
```

## Notifications Dropdown

A notifications dropdown for Tailwind CSS with an unread badge on the bell, a scrollable feed, and quick actions. Built with the [popover](/components/popover) and [item](/components/item) components.

```html preview preset=lg align=top open=.popover
%include examples/dropdowns/notifications.tsx%
```

## Country Dropdown

A searchable country dropdown for Tailwind CSS with flags, recent picks, and type-to-filter, submitting like a native select. Built with the [combobox](/components/combobox) component.

```html preview preset=lg align=top open=.combobox
%include examples/dropdowns/country-picker.tsx%
```

## Mega Menu

A mega menu for Tailwind CSS that opens on hover with grouped icon links, short descriptions, and an announcement column. Built with the [popover](/components/popover) component.

```html preview preset=lg align=top open=.popover
%include examples/dropdowns/mega-menu.tsx%
```

## Table Row Actions Dropdown

A table row actions dropdown for Tailwind CSS, opened from an ellipsis button on each row, with delete kept apart. Built with the [table](/components/table) and [dropdown](/components/dropdown) components.

```html preview preset=lg align=top open=.dropdown:first
%include examples/dropdowns/table-row-actions.tsx%
```
