---
title: Tailwind CSS Component Library
description: Beautiful, modern component library for Tailwind CSS. Use it with any stack, like React, Vue, Laravel, or plain HTML, no framework needed. Easily customizable.
---

Starting Point UI is a free, open-source Tailwind CSS component library that works with any web stack, no React required. You get beautiful, accessible components as simple semantic classes, with a small vanilla JavaScript module driving the interactive ones. It covers everything from [buttons](/components/button) and [forms](/components/forms) to [dialogs](/components/dialog), [dropdowns](/components/dropdown), [sidebars](/components/sidebar), and [chat UI](/components/message).

## An example

A profile dropdown with an account header, grouped shortcuts, and a nested submenu. The classes handle the design, the data attributes wire up the behavior, and the ARIA roles, keyboard navigation, and focus management come built in. Copy the code and it works.

```html preview preset=lg align=top open=.dropdown
%include examples/dropdowns/profile-dropdown.tsx%
```

## Why Starting Point UI?

- **Works everywhere**: React, Vue, Angular, Laravel, Rails, Django, or plain HTML. If it runs Tailwind, it works.
- **Easy to use**: add classes like `btn`, `card`, or `input` and you're done.
- **Accessible**: WAI-ARIA patterns, keyboard navigation, and focus management built in. Every example ships accessible markup you can copy as is.
- **Themeable**: colors, radius, and fonts are CSS variables, with dark mode out of the box. See [Customization](/guides/customization).
- **Lightweight**: just CSS and an optional JavaScript module.
- **Clean markup**: readable class names, no class soup.

## Inspired by shadcn/ui

If you came looking for a [shadcn/ui](https://ui.shadcn.com) alternative that doesn't require React, you came to the right place. The design system is shadcn/ui's, so a `btn`, a `card`, or a `dialog` looks and behaves like its React counterpart, and existing shadcn/ui themes drop straight in unchanged.

Starting Point UI could not exist without shadcn/ui and these other open source projects. Much of what you find here, from the design system to the way the JavaScript is structured, comes from using them, studying their source code, and learning from the people behind them. Be sure to check them out.

- [Tailwind CSS](https://tailwindcss.com)
- [shadcn/ui](https://ui.shadcn.com)
- [Radix UI](https://www.radix-ui.com)
- [daisyUI](https://daisyui.com)
- [Basecoat](https://basecoatui.com)
- [UIkit](https://getuikit.com)

Ready to get started? Head over to the [Installation](/guides/installation) guide.
